# Project Instructions

## Task completion / browser verification

When a coding task's implementation is finished, report it as complete without
automatically running Playwright browser verification. Instead, ask the user
(as a choice) whether they want verification run now.
